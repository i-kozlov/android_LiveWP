package com.baybaka.lievwp.android.model;

import com.badlogic.gdx.math.Vector2;

public class ParticleHelixDirection extends BaseParticle {

    public ParticleHelixDirection(Vector2 velocity, Vector2 acceleration, int lifetime, int size) {
        super(velocity, acceleration, lifetime, size);
    }

    @Override
    public void move(float delta) {
        //add later
    }
}
